import React from "react";
import { View } from "react-native";
import { pagination } from '../../../styles';

const Pagination = () => {
    return (
        <View style={pagination.container}></View>
    )
}
export default Pagination;